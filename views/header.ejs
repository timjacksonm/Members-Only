<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/reset.css" />
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css" />
    <title>Document</title>
  </head>
  <body>
    <header class="justify-content-between">
      <h1>
        <a href="/home" class="home">Members Only</a>
      </h1>
      <% if (user) { %>
      <div class="align-self-center">
        Welcome <%= user._doc.email %>, Status: <%= user._doc.status %>
      </div>
      <div class="flex">
        <% if (user._doc.status === 'Member' && url !== '/joinclub') { %>
        <p class="align-self-center">
          <a href="/joinclub">Join the club!</a>
        </p>
        <% } else if (user._doc.status === 'Club Member' && url !==
        '/becomeadmin' && !user._doc.admin) { %>
        <p class="align-self-center">
          <a href="/becomeadmin">Become an admin!</a>
        </p>
        <% } %>
        <p class="align-self-center">
          <a href="/logout">logout</a>
        </p>
      </div>
      <% } else { %>
      <p class="align-self-center">
        <a href="/login">login</a>
      </p>
      <% } %>
    </header>
  </body>
</html>
